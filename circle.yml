machine:
  services:
    - docker

test:
  override:
    - docker-compose run -d www
    - mocha

deployment:
  hub-master:
    branch: master
    commands:
      - $DEPLOY_MASTER
  hub-develop:
    branch: develop
    commands:
      - $DEPLOY_DEVELOP
